# 가상환경 폴더 (이미 잘 하셨어요)
venv/
env/
.venv/

# 파이썬 캐시 파일 (필수 ⭐)
__pycache__/
*.pyc

# 데이터베이스 파일 (수정됨)
# movies.db 같은 바이너리 파일은 깃허브에 올리지 않는 것이 원칙입니다.
*.db
*.sqlite3

# VS Code 설정 폴더
.vscode/

# 운영체제 임시 파일 (윈도우/맥)
.DS_Store
Thumbs.db

# 환경변수/비밀번호 파일 (보안상 중요)
.env